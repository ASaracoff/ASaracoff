// ES2015
// Definitions
    // ES2015: most recent vrs of JS
        // significant changes and upgrades
    // Destructuring: 
        // extracting multiple values from data stored in (maybe nested) objects and arrays
    // Map: new data strucure
        // similar to objects in SJ allow us to store elements in key/value pairs
        // Map: inseter in 'insertion order'
            // CAN contain objects, functions and other datatypes as KEYS
    // Set: JS collection of UNIQUE values
        // each value can only occur 1x in a set
        //  can hold ANY value of ANY data type

// Intro
    // newer JS features
    // some aren't supported in IE

// brief history of JS
    // 1990 competing browser
    // no common programming lang
    // 1995 NetScape: Brendan Eich 
        // created and implemented JS in 10 DAYS! WoW
        // Microsoft reversed engineered it -> JScript
        // first Browser WAR! 
    // NetScape proposed to make a universal programming lang = ECMAScript

// ECMAScript 
    // the STANDARD for JS lang
    // not a programming lang itself just the rules?
    // how a lang SHOULD work
    // made languages UNIFIED
    // 2013 browsers FINALLY complied
    // browsers/Nodes implement the lang based on THIS standard

// ECMA?
    // "European Computer Manufacturers Association" 
        // OG NAME
    // changed: ECMA International
        // european association for standardizing info and communication systems
    // standard org for info and communucation systems
    // very sad website 2019
        // website as of 2025 VERY nice
        // ECMA JS standards vrs 16 June 2025
        // https://ecma-international.org/publications-and-standards/standards/ecma-262/
    // makes standards throughout many technology based industries and areas
    // who makes the decisions?

// ECMA Script
    // dry text
    // no code
    // only the rules

    // function definitions
        // break down of what the syntax should look like

// Who makes the decisions? TC39
    // they are named in numerical order
        // Tecnical Commitee 39
    // maintains and updates the ECMAScript standard
    // painstaking and CAREFUL process
    // can't have radical changes
        // older websites need to be still maintained
        // backwards compatible
    // browser companies ceos, big programmers, people that are mainly effected by programming changes

// Evolving JS - the old way
    // New features were batched together
        // large releases
    // releases named w version # (ES3,ES5)
    // versions released infrequently - often years apart
    // features that were reay had to wait on standby uptil ENOUGH were collected to be implemented

// Evolving JS - The NEW way
    // updates published YEARLY
    // new features ready each year are adde together
    // versions named by year (ES2016, ES2017, etc)
    // ES6 first release under the yearly option
    // meant to be more open and available
    // had a github account created

// A new Formalized proposal process
    // very complex
    // 4/5 stages
    // very slow process

// Stage 0: strawman
    // the idea
    // freeform idea without too much effort
        // "what if...."

// Stage 1: Proposal
    // more expectations
    // criteria
    // chamion of TC39 to recognize it's value
    // examples of usage
    // propose outlining the problem/need and the general shape of a solution

// Stage 2: Draft
    // still to early for us to cover!
    // sometimes implementers will experiment addiing this to browsers.environments
    // more merit than stage 1
    // more requirements

// stage 3: Candidate
    // proposals are candidate recommendations
    // unlikely to change beyond fies to issues identified in the wild
    // proposals move here with at least 1 browser implementation OR when supported by a build-time trasnpiler like Babel
    // Chrome has a feature that allows user to use stage 3 things
    // most likely abel to be used eventually

// Stage 4: Finished
    // proposals here have at least 2 independent implementations that pass acceptance tests
    // 2+ spec editors have to sign off on it
    //  has been implemented by almost all modern browsers

// We don't need to really know this but good to know that everything newely added features are thoroughly tested and scrutinized
// they CAN be trusted

// passed features
